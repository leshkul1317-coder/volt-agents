# ВОЛЬТ — состояние проекта (хендовер между чатами)

Дата: 2026-09-04. Этот файл — «память» для нового чата. Прочитайте его первым.

## Бизнес
ВОЛЬТ — прокат (аренда) электроинструмента бренда **APEXWOLT** в **Красноярске**.
Ассортимент: перфораторы, гайковёрты, УШМ, дрели-шуруповёрты, лобзики, строит. фены.
Оффер: аренда сутки/3 дня/неделя/месяц, возвратный залог, **бесплатная доставка по
Красноярску при аренде от 3 дней**. Аудитория: частники (дача/дом/ремонт) и профи.
Голос бренда: премиальный, лаконичный; слоган «Сила в аренду»; чёрный `#0A0A0A` +
жёлтый `#EDAB18`. Всё на русском.

## Три рабочих пространства (держим раздельно)
1. **Сайт** — папка `…/Desktop/webbings`. Задеплоен: **https://volt-rent.netlify.app**
   (Netlify, публичный). Деплой: `netlify.cmd deploy --build --prod` из папки сайта
   (собирается чистый `dist`, функции — из `netlify/functions`). Печатные макеты:
   `webbings/print/` (визитка + листовка), готовые PDF присылались в чат.
2. **Telegram-боты** — код в `webbings/netlify/functions/`:
   - `order.js` — заказ с сайта → курьеру в личку. Env: `TELEGRAM_BOT_TOKEN`,
     `COURIER_CHAT_ID`. Инструкция: `webbings/TELEGRAM-BOT.md`. Работает ✅.
   - `support.js` — саппорт-бот (сценарный: меню + FAQ + поиск по словам +
     эскалация/релей владельцу). Env: `SUPPORT_BOT_TOKEN`, `OWNER_CHAT_ID`,
     `SUPPORT_WEBHOOK_SECRET`. Webhook привязан. Инструкция: `webbings/SUPPORT-BOT.md`.
     Работает ✅.
   - Секреты — только в переменных окружения Netlify (в коде их нет).
3. **ИИ-агенты** — ЭТА папка `volt-agents`. 4 маркетинговых агента
   (`.claude/agents/`: marketolog, kontent-smm, seo, analitik) + 50 навыков
   (`.claude/skills/`). Контекст — в `CLAUDE.md`, памятка — в `README.md`.

## Что сделано за 2026-09-04
- Правки сайта: убран «Листайте вниз»; hero-видео заменено (muzik.mp4), сжато
  ffmpeg 18.8→4.1 МБ, замирает на последнем кадре; «В АРЕНДУ» одной строкой;
  фиксы мобильной вёрстки (баннер доставки, вакансии) и тарифов на товаре.
- Заведены оба Telegram-бота (order + support) и задеплоены на volt-rent.
- Собрана маркетинг-команда агентов (этот проект).

## Следующая задача (продолжить в новом чате)
▶️ **Запустить всех 4 агентов, чтобы выдали единый план продвижения на 90 дней
под Красноярск** (фазы 0–30 / 31–60 / 61–90):
- marketolog — стратегия, KPI, офферы/промо, приоритеты каналов и бюджета;
- kontent-smm — контент-план и SMM (VK, Telegram, Reels, Avito, 2ГИС), примеры постов, email/SMS;
- seo — ключи и локальное SEO (Яндекс.Бизнес, 2ГИС, schema, посадочные);
- analitik — метрики, воронка, A/B-тесты, CRO оформления заказа.
Затем собрать в один документ (напр. `plan/plan-90-krasnoyarsk.md`).

## Позже
- Расширить команду: стратег/советник (C-level), операционный, продажи — из набора
  github.com/alirezarezvani/claude-skills (см. `README.md`, раздел 6–7).
- Телефон в макетах/боте пока `xxx` — подставить реальный, перегенерировать PDF/QR.

## Заметки по инструментам (стоят локально)
- `netlify-cli` (деплой), `ffmpeg-static` и `qrcode` (во временной папке сессии —
  при необходимости переустановить), Chrome headless — для генерации PDF макетов.
- PowerShell блокирует `netlify.ps1` → используйте **`netlify.cmd ...`**.
